// Serves up the initial /droids page element tag
//to then attach all of the other components to

const droid_search = () => {
  return `<div id="droid-search">
  <section id="droid-container"> </section>
  </div>`;
};
